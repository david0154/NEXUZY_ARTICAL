# Core Dependencies
Firebase-admin>=6.2.0
pyrebase4>=4.7.0
pyftpdlib>=1.5.7

# Export Dependencies
reportlab>=4.0.0
openpyxl>=3.1.0

# UI (Tkinter is built-in with Python)
# No additional UI dependencies needed
